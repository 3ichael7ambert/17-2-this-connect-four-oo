//EXTRA - Not Functioning

// let element = document.getElementById('gradientBackground');
// let colors = ['#ffeb3b', '#ff9800', '#f44336', '#9c27b0', '#3f51b5', '#03a9f4', '#00bcd4', '#8bc34a', '#ffeb3b'];
// let i = 0;

// setInterval(changeGradientColor, 1000);

// function changeGradientColor() {
//   element.style.background = `radial-gradient(circle, ${colors[i]}, ${colors[(i+1)%colors.length]}, ${colors[(i+2)%colors.length]})`;
//   i = (i + 1) % colors.length;
// }
